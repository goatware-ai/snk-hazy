# Rules

Generated by `autoeval_check.py --rules` on 2026-09-21. Do not edit by hand: the statement on each line is the first line of the check's docstring in tools/gcheck/, and the grouping is tools/gate_families.py. Regenerate after any check or family change.

194 checks, 261 check ids, 38 families, 19 procedural rules.

## Stage 1 · In-app pre-submission checks

*before submitting; free to fix.*

### 1a Prompt / O*NET / Name in-app checks

#### PRE-FRAME: The prompt's opening frames the US setting, the requester's role and the expertise the reader brings, in the requester's own voice.

- **P4** — The prompt's opening frames the US setting, the requester's role and the expertise the reader brings.
  <sub>check_prompt_role_and_locale · needs prompt</sub>
- **P6** — The prompt's frame is woven into the requester's own voice, never written as a self-introduction to a coworker.
  <sub>check_prompt_self_introduction · needs prompt</sub>

#### PRE-OCC: The domain, occupation and the form's own metadata fields are all on the platform's closed lists and agree with each other.

- **M1** — the occupation code is one of the platform's 64
  <sub>check_metadata · needs metadata</sub>
- **M2** — the domain is one of the platform's 14 and matches the occupation's O*NET job family
  <sub>check_metadata · needs metadata</sub>
- **M3** — the recorded occupation title matches the code, and the metadata carries no legacy "sector" key
  <sub>check_metadata · needs metadata</sub>
- **M4** — four integer minute fields, and a total in hours at least their sum and over 3
  <sub>check_form_fields · needs metadata</sub>
- **M5** — at least one non-AI tool is logged
  <sub>check_form_fields · needs metadata</sub>
- **M6** — at least 2 input files are present, 3+ preferred
  <sub>check_form_fields · needs metadata</sub>

#### PRE-FILES: The prompt names the deliverable file it expects, explicitly.

- **P0** — the task folder carries a prompt.md
  <sub>check_prompt_rules · needs inputs, prompt, solution</sub>
- **P1** — the prompt names the deliverable file, with an output-naming cue (saved as / named / call it) in the 80 chars before it
  <sub>check_prompt_rules · needs inputs, prompt, solution</sub>

#### PRE-SOURCES: The prompt names at least one concrete input source.

- **P2** — the prompt names at least one input file
  <sub>check_prompt_rules · needs inputs, prompt, solution</sub>

#### PRE-VERBOSE: The prompt does not catalogue its inputs.

- **P3** — the prompt does not introduce five or more inputs, 70% or more of them, as '... is <filename>'
  <sub>check_prompt_rules · needs inputs, prompt, solution</sub>
- **P5** — at most half of the input files the prompt names carry an individual content or purpose gloss
  <sub>check_prompt_rules · needs inputs, prompt, solution</sub>
- **P7** — No paragraph of the prompt opens three or more sentences on the same two-word stem.
  <sub>check_repetitive_openers · needs prompt</sub>

#### PRE-UNIQ: The prompt does not reuse a prior task's reasoning path.

- **U1** — The prompt reuses neither the bulk wording nor the signature narrative moves of a prior prompt in the catalogue.
  <sub>check_recycling · needs folder, originality, prompt</sub>
- **U2** — The prompt reuses neither the bulk wording nor the signature narrative moves of a prior prompt in the catalogue.
  <sub>check_recycling · needs folder, originality, prompt</sub>

#### PRE-PACK: Both zips are flat, every name is clean, no file is empty, and every file the prompt names exists.

- **H4** — no space or double extension in a name, no empty file, no subfolder, space or empty member in a zip
  <sub>check_hygiene · needs inputs, solution</sub>
- **H5** — every file the prompt names exists in the folder
  <sub>check_hygiene · needs inputs, solution</sub>
- **H6** — no placeholder or dummy phrase (sample data, dummy text, placeholder, lorem ipsum, TBD) in an input file, PDFs read too
  <sub>check_hygiene · needs inputs, solution</sub>
- **H7** — A reference CSV input, a dated series or a sorted unique-id lookup, is 40 lines or fewer, laid out in side-by-side panels if it is longer, because the platform previews the first 25 and last 15 lines of a CSV and reads the hidden middle as missing rows.
  <sub>check_previewable_series · needs inputs</sub>
- **H8** — Every input row keyed by an id the golden cites, or carrying a decimal figure the golden types into a cell that no input shows inside its own preview, sits inside the platform's preview window, the first 25 rows of a workbook sheet or the first 25 and last 15 lines of a CSV, when the file is longer than 40 rows; a cited row outside it is reported as absent whatever the golden says about it.
  <sub>check_cited_rows_in_window · needs inputs, solution</sub>
- **H9** — No field in an input CSV contains a comma, because the platform splits CSV lines on commas without honouring quotes and reads every column after a quoted comma as shifted.
  <sub>check_csv_fields_without_commas · needs inputs</sub>

#### PRE-NAME: No project or evaluation reference anywhere in the shipped package.

- **H1** — the programme codename appears in no file of the folder (every member of every Office package, every text file)
  <sub>check_meta_references · needs inputs, prompt, rubric, solution</sub>
- **N1** — no input or solution file carries project/eval vocabulary in its content, XML parts or name, and no file name reads as a category or AI-output label
  <sub>check_meta_references · needs inputs, prompt, rubric, solution</sub>

### 1b Rubric Quality Check (structure, objectivity, polarity, near-identical, requirement mapping, full-credit completeness)

#### PRE-OBJ: Criteria state checkable facts — no evaluative words, hedges, grader instructions, or golden-only cell references.

- **R18** — A criterion states a fact about the deliverable, never an instruction telling the grader when to pass or fail.
  <sub>check_grader_instructions · needs rubric</sub>
- **R19** — A criterion carries no unanchored hedge-like qualifier that defers its scope to unspecified judgment.
  <sub>check_hedges · needs rubric</sub>
- **R25** — an evaluative term (consistently, appropriate, thin ...) carries a figure, quote or comparison in the same sentence
  <sub>check_form_basics · needs rubric</sub>
- **R119** — A positive row over a computed money or quantity figure names the expected value or a concrete correctness check, never presence alone.
  <sub>check_presence_only_computed · needs rubric</sub>

#### PRE-POL: Negatives carry a polarity pin and frame the defect affirmatively.

- **E1** — a negative carries a polarity pin (a defect-frame word)
  <sub>check_rubric_lint · needs rubric</sub>
- **E2** — a negative never opens with a bare plural subject
  <sub>check_rubric_lint · needs rubric</sub>
- **W9** — no meet-style polarity pin ('met only when')
  <sub>check_rubric_lint · needs rubric</sub>
- **W14** — a negative stacks at most two negations outside its pin
  <sub>check_rubric_lint · needs rubric</sub>
- **W15** — a negative never sweeps how a figure was computed or which column it came from
  <sub>check_rubric_lint · needs rubric</sub>
- **W17** — a negative's defect is never the application of a named rule to a record
  <sub>check_rubric_lint · needs rubric</sub>
- **W18** — a negative's defect sentence carries a defect-frame word
  <sub>check_rubric_lint · needs rubric</sub>
- **W19** — a negative's main verb is one a compliant deliverable never performs
  <sub>check_rubric_lint · needs rubric</sub>

#### PRE-SCOPE: A penalty is spent only on a critical commission.

- **R69** — a negative never has a positive's subject-and-verb signature, nor a conclusion on the same subject matter
  <sub>check_mirrors · needs rubric</sub>
- **R104** — a positive and a negative never share one rule anchor over the same governed objects
  <sub>check_mirrors · needs rubric</sub>

#### PRE-DUP: No criterion scores what another criterion already scores.

- **R10** — two criteria never key the same golden cell, or a range containing another's cell
  <sub>check_subsumption · needs rubric</sub>
- **R15** — a negative shares no two record ids (PO, registration, catalog numbers) with a positive
  <sub>check_mirrors · needs rubric</sub>
- **R22** — a negative restates no distinctive figure a positive already scores
  <sub>check_mirrors · needs rubric</sub>
- **R41** — a negative's operative sentences share no distinctive proper noun with a positive
  <sub>check_mirrors · needs rubric</sub>
- **R47** — a single-figure positive never states only a figure another positive also states
  <sub>check_subsumption · needs rubric</sub>
- **R57** — a positive asserting a derivation over every row never coexists with positives scoring named rows of it
  <sub>check_subsumption · needs rubric</sub>
- **R60** — one named quantity is never equated to two different figures
  <sub>check_subsumption · needs rubric</sub>
- **R86** — two positives never both assert the presence of the same attribute per item on the same subject with no shared figure
  <sub>check_subsumption · needs rubric</sub>
- **R95** — A dollar total is asserted as a scored total in one criterion only.
  <sub>check_total_twins · needs rubric</sub>
- **R116** — Two positives never both carry the same "all N <noun>" completeness phrase; the second states its own point without restating the count.
  <sub>check_repeated_completeness_phrase · needs rubric</sub>
- **R124** — No two criteria score one thing twice: no overlapping answer keys, no single-figure row another restates, no universal derivation over rows other positives name, no attribute asserted per item twice, and no named quantity equated to two figures.
  <sub>check_subsumption · needs rubric</sub>

#### PRE-COVER: Completeness is pinned, and full credit is unreachable without the derivation.

- **R4** — a prompt demanding every/all items has a criterion pinning a completeness count
  <sub>check_prompt_every_coverage · needs prompt, rubric</sub>
- **R13** — the coverage criteria together carry at least 10% of positive weight
  <sub>check_prompt_every_coverage · needs prompt, rubric</sub>
- **R24** — a workbook with every figure hand-keyed keeps under 85% of positive weight, and no single strict liveness row alone leaves 90% within a point of drift
  <sub>check_completeness_share · needs prompt, rubric, solution</sub>
- **R73** — when the prompt demands live formulas throughout (or a rerun on refreshed inputs), the gated tier alone (per-row gated rows for a rerun) holds over 15% (10% plus a point) of positive weight
  <sub>check_completeness_share · needs prompt, rubric, solution</sub>
- **R74** — A prompt that sells dynamic recalculation has formula criteria reaching past single cells to the chains that recalculate.
  <sub>check_chain_scope · needs prompt, rubric</sub>
- **R76** — A strict liveness criterion names the golden's mechanism and admits any live alternative in the same sentence, never plain cell references alone.
  <sub>check_tactic_lock · needs rubric</sub>
- **R83** — Every deliverable basename the prompt names appears verbatim in at least one positive criterion.
  <sub>check_filename_coverage · needs inputs, prompt, rubric</sub>
- **R92** — a batched-by or grouped-by presentation the prompt demands has a positive criterion of its own
  <sub>check_prompt_every_coverage · needs prompt, rubric</sub>
- **R101** — Every labelled block on the golden's front page is scored by at least one criterion.
  <sub>check_front_page_blocks_scored · needs rubric, solution</sub>
- **R102** — When the prompt names who the deliverable is addressed to, a positive criterion grades the addressee.
  <sub>check_addressee_graded · needs prompt, rubric</sub>
- **R115** — A prompt asking for a figure by one dimension and another has a positive criterion carrying a breakdown cue for both dimensions.
  <sub>check_two_way_breakout · needs prompt, rubric</sub>
- **R126** — Every golden aggregation total's labelled output is graded by at least one positive criterion, never by a negative alone.
  <sub>check_output_totals_have_positives · needs rubric, solution</sub>
- **R127** — A golden column of verdict words under a TEST / CHECK / FLAG / VERDICT / CALL header is a rule the golden applies, and at least one positive criterion names that header or one of its verdict words.
  <sub>check_verdict_column_coverage · needs rubric, solution</sub>
- **R132** — A rounding rule the prompt or an input states is graded on the value it produces, never only on conditions a higher value also meets.
  <sub>check_rounding_rule_graded_on_value · needs inputs, rubric</sub>
- **R134** — Every deliverable ask in the prompt is mapped in the task's clause-map.md to a clause quoted verbatim from the prompt, a quoted anchor the golden contains and at least one positive rubric row, and once three returns are logged the map carries a Rebuilt date on or after the latest one.
  <sub>check_clause_map · needs folder, prompt, rubric, solution</sub>

#### PRE-FORM: The submission form's own bands are respected.

- **E0** — WEIGHT is numeric
  <sub>check_rubric_lint · needs rubric</sub>
- **R1** — a criterion is at most 500 characters (the form cap)
  <sub>check_form_basics · needs rubric</sub>
- **R11** — The rubric carries at least 6 criteria, and 20+ for anything but a simple task.
  <sub>check_criterion_count · needs rubric</sub>
- **R12** — Every weight is a non-zero integer in -5..+5.
  <sub>check_weight_bands · needs rubric</sub>
- **R61** — Negative weight reaches 20% of positive weight (a recommendation, never a gate).
  <sub>check_penalty_share · needs rubric</sub>
- **R99** — At least one positive criterion carries +4 or +5.
  <sub>check_flat_weighting · needs rubric</sub>
- **R135** — The rubric's LAST criterion is the general "Overall formatting and style of the deliverable" line.
  <sub>check_closing_style_criterion · needs rubric</sub>
- **R136** — No criterion asserts a hedged figure; every value is pulled exactly from the ground truth.
  <sub>check_hedged_figures · needs rubric</sub>

#### PRE-FOCUS: Criteria grade the finished file, not the order the work was done in.

- **R2** — no process-ordering clause; a finished file carries no drafting timeline
  <sub>check_form_basics · needs rubric</sub>

## Stage 2 · Post-submission evals (EVALUATION_PENDING → NEEDS_REVISION)

*60-120 min after submitting; costs a submission cycle.*

### 2a golden_solution_check (3 agents, must score 1.0000/1.0000/1.0000)

#### GOLD-LAND: Every figure a criterion asserts lands on ONE cell the judge can grep.

- **R20** — every stated figure is a cached cell value somewhere in the golden
  <sub>check_stated_figures · needs rubric, solution</sub>
- **R21** — one sheet carries all of a criterion's figures
  <sub>check_stated_figures · needs rubric, solution</sub>
- **R26** — every stated count is held by a golden cell (or carried in a cell's words)
  <sub>check_stated_counts · needs rubric, solution</sub>
- **R31** — a count the criterion says stands in a cell is a numeric cell, not prose
  <sub>check_stated_counts · needs rubric, solution</sub>
- **R32** — no exact count of figures or author-composed list items on a summary page
  <sub>check_presentation_counts · needs rubric</sub>
- **R35** — A one-row attribute claim is keyed on a token that only one sheet carries.
  <sub>check_landable_literals · needs rubric, solution</sub>
- **R43** — Every sheet that carries a criterion's pinned money figure carries the rest of that criterion's figures too.
  <sub>check_figure_colocation · needs rubric, solution</sub>
- **R48** — A criterion asserting N percent lands on a cell whose stored value reads N, not N/100.
  <sub>check_percent_anchors · needs rubric, solution</sub>
- **R50** — a figure whose trailing zero lives only in the number format is quoted as stored (10.8, not 10.80)
  <sub>check_padded_figures · needs rubric, solution</sub>
- **R51** — A criterion's figure appears only once on the named row it is bound to.
  <sub>check_row_figure_twins · needs rubric, solution</sub>
- **R56** — a date pinned to a row key sits beside that key on some golden sheet, in the form the criterion writes it
  <sub>check_date_key_colocation · needs rubric, solution</sub>
- **R58** — A cause a criterion attributes to a named line is confirmable from that line's own row.
  <sub>check_cause_attribution · needs rubric, solution</sub>
- **R59** — A figure a criterion states is totalled once per sheet, never twice by different aggregations on two total rows.
  <sub>check_figure_sheet_twins · needs rubric, solution</sub>
- **R62** — A direction claim is made only on a figure the golden holds with one sign.
  <sub>check_direction_sign_twins · needs rubric, solution</sub>
- **R63** — A criterion quoting =Sheet! names a sheet the workbook carries.
  <sub>check_named_sheet_exists · needs rubric, solution</sub>
- **R64** — a total pinned as a percent of a base states the base figure inline
  <sub>check_stated_basis · needs rubric, solution</sub>
- **R65** — A positive states a price move as one figure, never as a new price beside the old one.
  <sub>check_two_price_moves · needs rubric</sub>
- **R66** — The row carrying a criterion's pinned figure states the same count the criterion does.
  <sub>check_landing_row_counts · needs rubric, solution</sub>
- **R70** — A pinned row count equals the data-row count of one golden table.
  <sub>check_docx_row_count_landing · needs rubric, solution</sub>
- **R78** — A criterion never pins a count of solver-authored plan items (actions, steps, recommendations).
  <sub>check_plan_counts · needs rubric</sub>
- **R79** — A placement claim ('beside', 'below') names the literal label next to the pinned value, never an abstract referent.
  <sub>check_placement_claims · needs rubric</sub>
- **R80** — A positive comparative ('rather than', 'instead of') makes the deliverable its subject and names who did what, never the audited object alone.
  <sub>check_agentless_comparative · needs rubric</sub>
- **R87** — a figure pinned as N percent of something reproduces from a golden value at that rate to the cent
  <sub>check_stated_basis · needs rubric, solution</sub>
- **R91** — a criterion points at the count cell rather than saying 'carries N items' with the count standing in a cell
  <sub>check_presentation_counts · needs rubric</sub>
- **R93** — a negative quoting a month token (MM/YYYY) names an item or lot code on the row carrying the exact token
  <sub>check_date_key_colocation · needs rubric, solution</sub>
- **R94** — a raw-form figure the prose spells padded is anchored on the sheet or row label holding the cell
  <sub>check_padded_figures · needs rubric, solution</sub>
- **R103** — A positive enumerating five or more items on a prose tab has at least that many short cells there, one per item, for the judge to count.
  <sub>check_enumeration_on_prose_tab · needs rubric, solution</sub>
- **R108** — A positive that promises a citation of an input file by name lands only on that file name in the golden's own text.
  <sub>check_cited_file_named_in_golden · needs inputs, rubric, solution</sub>
- **R120** — When a positive row says first page or front page, the workbook's first worksheet is that page.
  <sub>check_first_page_is_first_sheet · needs rubric, solution</sub>
- **R123** — An identifier the golden borrows from an input (a feature code, a document number, a credit memo id) is written in the input's exact hyphenated form, never with a space or a dash variant in place of the hyphen.
  <sub>check_borrowed_identifier_exact_form · needs inputs, prompt, solution</sub>
- **R131** — A positive criterion citing a numbered section ("Section 3.2", "PM-4 3.3") finds that cite in the golden at the head of a cell or inside a short cell; a cite that occurs only past the first 200 characters of 500-character prose cells is read by the judge as the sentence around the hit and the row fails.
  <sub>check_section_cite_past_read_head · needs rubric, solution</sub>
- **R133** — A decimal figure a positive pins is the single rounding of its unrounded inputs, and that unrounded value does not sit on a tie at the places the figure is printed to.
  <sub>check_pinned_figure_rounding_tie · needs rubric, solution</sub>
- **W5** — a 'may be typed' carve-out pins its dollar value
  <sub>check_rubric_lint · needs rubric</sub>
- **W8** — a dollar spot-check beside sibling cost columns carries an answer-key anchor
  <sub>check_rubric_lint · needs rubric</sub>
- **W13** — a positive never asserts blankness or absence as the observable
  <sub>check_rubric_lint · needs rubric</sub>

#### GOLD-LIVE: Liveness is anchored on a cached value; it is never asserted on its own.

- **R6** — a criterion cites no function name: the judge greps cached values, never formula text, and fails on a token it cannot find
  <sub>check_named_functions · needs rubric, solution</sub>
- **R7** — a keyed range asserting a function stores pure single-call formulas, never compound ones
  <sub>check_rubric_keys · needs rubric, solution</sub>
- **R9** — a formula-only liveness criterion carries a displayed-value anchor
  <sub>check_form_basics · needs rubric</sub>
- **R16** — A golden total cell stores one pure aggregation, never an aggregation plus further terms.
  <sub>check_compound_totals · needs solution</sub>
- **R17** — A rubric carries fewer than three formula-only liveness positives, since each one multiplies the flake exposure.
  <sub>check_liveness_count · needs rubric</sub>
- **R30** — A strict liveness criterion names a plain cell reference or a head function for the judge to land on.
  <sub>check_liveness_landing · needs rubric</sub>
- **R36** — A criterion stating how many columns or rows a total sums matches the stored SUM range of the cell carrying that total.
  <sub>check_sum_span · needs rubric, solution</sub>
- **R38** — a liveness criterion naming a sheet lands on a sole-call head function it names, or a reference-chain cell, that the sheet holds
  <sub>check_tab_sweep_liveness · needs rubric, solution</sub>
- **R39** — a whole-tab liveness sweep never names a tab holding typed numeric constants
  <sub>check_tab_sweep_liveness · needs rubric, solution</sub>
- **R42** — A strict liveness anchor value sits in a cell that is one pure function call or a plain read-through, never a compound formula.
  <sub>check_liveness_anchor_cell · needs rubric, solution</sub>
- **R44** — liveness is graded on plain read-through cells, never on a range storing a named function
  <sub>check_named_functions · needs rubric, solution</sub>
- **R45** — A liveness criterion never anchors on a money figure another criterion already scores.
  <sub>check_liveness_anchor_overlap · needs rubric</sub>
- **R46** — A liveness anchor is never a COUNTIF over a text pattern that many cells on the sheet match.
  <sub>check_anchor_twins · needs rubric, solution</sub>
- **R72** — Strict reference-chain liveness is pinned on a value whose cell is a reference or one aggregation, never arithmetic.
  <sub>check_liveness_anchor_cells · needs rubric, solution</sub>
- **R89** — No money-sized literal is nested inside a stored golden formula.
  <sub>check_nested_literals · needs solution</sub>
- **R90** — a liveness criterion names no spreadsheet function token
  <sub>check_named_functions · needs rubric, solution</sub>
- **R96** — A cell a plain reference points at never stores a SUMPRODUCT multiplying a comparison when the rubric demands live computation.
  <sub>check_readthrough_targets · needs rubric, solution</sub>
- **R97** — When the prompt asks for a per-month view and a golden sheet splits its figures by month, some criterion pins one month's figure from it.
  <sub>check_month_split_scored · needs prompt, rubric, solution</sub>
- **R98** — The cell a strict liveness row is about has the shape the row claims.
  <sub>check_liveness_subject_cell · needs rubric, solution</sub>
- **R106** — A strict liveness row's value anchor is labelled in the cell's own words, never a bare noun the same sentence uses for another quantity.
  <sub>check_liveness_anchor_label · needs rubric</sub>
- **R109** — A gated liveness clause never claims a date cell is a formula when the golden renders dates as text through TEXT().
  <sub>check_gated_clause_on_text_date · needs rubric, solution</sub>
- **R117** — Two strict liveness rows resolve to cells on two different sheets, one deliverable's output each.
  <sub>check_strict_pair_sheets · needs rubric, solution</sub>
- **R118** — At most one positive row is a formula-only gated clause with no figure; further gated clauses ride on value rows.
  <sub>check_formula_only_gated_rows · needs rubric</sub>
- **R125** — A gated liveness clause on a count never lands on a lone COUNT cell at the foot of a column whose other formulas share a different head.
  <sub>check_lone_count_footer · needs rubric, solution</sub>
- **W4** — a formula-liveness positive accepts reference chains
  <sub>check_rubric_lint · needs rubric</sub>
- **W7** — no counterfactual (recompute-on-change) wording on a static file
  <sub>check_rubric_lint · needs rubric</sub>
- **W10** — a chain- or hop-framed liveness criterion says no tracing is needed
  <sub>check_rubric_lint · needs rubric</sub>
- **W11** — a universal sweep ('every/all ...') carries an answer-key anchor
  <sub>check_rubric_lint · needs rubric</sub>
- **W16** — a liveness demand is never written as a row sweep
  <sub>check_rubric_lint · needs rubric</sub>

#### GOLD-KEY: Answer keys resolve exactly, and once, to the delivered workbook.

- **R5** — a key names a sheet the task workbooks carry, inside its used area, and its asserted or quoted formula is what the cell stores
  <sub>check_rubric_keys · needs rubric, solution</sub>
- **R8** — A criterion that states a numeric answer and keys a single golden cell states that cell's cached value.
  <sub>check_key_values · needs rubric, solution</sub>

#### GOLD-NEG: A negative names one verdict cell — never a comparison, threshold or sweep.

- **R28** — A criterion's 'X rather than Y' pair is stated in the order the deliverable states it.
  <sub>check_inverted_pairs · needs rubric, solution</sub>
- **R33** — A workbook-wide property claim has no counterexample anywhere in the golden.
  <sub>check_universal_claims · needs rubric, solution</sub>
- **R34** — A rule stated over a class names a member the judge can land on.
  <sub>check_rule_anchors · needs rubric</sub>
- **R37** — A threshold negative has no golden cell already across its line.
  <sub>check_negative_thresholds · needs rubric, solution</sub>
- **R40** — A comparison negative lands on a single verdict cell.
  <sub>check_comparison_negatives · needs rubric, solution</sub>
- **R52** — A membership negative never collides with the golden's own prose about that member.
  <sub>check_membership_negatives · needs rubric, solution</sub>
- **R53** — A negative criterion spends exactly one negation.
  <sub>check_negation_count · needs rubric</sub>
- **R77** — A fabrication negative names a closed class of invented content, never an open class of figures the judge must confirm one by one.
  <sub>check_open_scope_fabrication · needs rubric</sub>
- **R111** — A negative names the deliverable as the actor of the defect; a passive opening ("<object> is incorrectly <done>") reads to the Rubric Quality Review as a description of a correct deliverable and is failed as inverted polarity.
  <sub>check_negative_passive_opening · needs rubric</sub>
- **W1** — no 'appears on no line' wording, which breaks on zero-quantity rows
  <sub>check_rubric_lint · needs rubric</sub>
- **W3** — the judge is never asked to re-sum an aggregate from a raw input CSV
  <sub>check_rubric_lint · needs rubric</sub>
- **W6** — no negative liveness mirror (formulas-vs-constants as a penalty)
  <sub>check_rubric_lint · needs rubric</sub>

#### GOLD-FID: The golden's own fabric agrees with itself across every tab.

- **G1** — No solution row carries a stays-as-placed status in one cell and a change instruction in another.
  <sub>check_action_contradiction · needs solution</sub>
- **G2** — No aggregation's range is empty of anything it could have aggregated.
  <sub>check_unreproducible_cache · needs solution</sub>
- **G3** — A front-page column of cross-sheet references whose head is the total of the lines under it ties, or carries a reconciliation cell for the difference.
  <sub>check_summary_reconciliation · needs solution</sub>
- **G4** — Every input register line billed under the golden's own agreement price appears on a golden row pairing the order and item with the correction.
  <sub>check_register_shortbill · needs inputs, solution</sub>
- **G5** — A count claim in the golden's prose is consistent with itself: in "X of the Y" X never exceeds Y, and the parts of a total split "in two forms" (ways, kinds, groups, parts) sum to that total or the paragraph states the overlap.
  <sub>check_narrative_count_claims · needs solution</sub>
- **G6** — A literal parameter whose source cell names a section of an input document appears in that section.
  <sub>check_parameter_provenance · needs inputs, solution</sub>
- **G7** — Every solution docx table with a total row sums, column by column, to that row.
  <sub>check_docx_total_rows · needs solution</sub>
- **G8** — When the prompt says who signs the deliverable, the golden carries a signature line.
  <sub>check_signature_block · needs prompt, solution</sub>
- **G9** — The deliverable is dated no later than the actions it schedules after the inputs' clock.
  <sub>check_actions_before_document_date · needs inputs, solution</sub>
- **G10** — Every invoice, PO or work-order style identifier the golden cites appears in an input.
  <sub>check_cited_identifiers · needs inputs, prompt, solution</sub>
- **G11** — A deadline the golden attributes to a named person carries a date that appears in a document mentioning that person.
  <sub>check_attributed_dates · needs inputs, solution</sub>
- **G12** — List every condition or prohibition the inputs state, for the builder to tick off against the golden (information only).
  <sub>check_input_conditions · needs inputs</sub>
- **G13** — List every item carrying more than one unit cost across the inputs, so a blended cost prices each source at its own (information only).
  <sub>check_multiple_unit_costs · needs inputs</sub>
- **G14** — Every account a golden dispute or claim tab adjudicates is one the correspondence names.
  <sub>check_dispute_entities · needs inputs, solution</sub>
- **G15** — Every person the golden assigns an action to is named in the prompt or an input.
  <sub>check_owner_names_in_inputs · needs inputs, prompt, solution</sub>
- **G16** — Every person a memo author line, an attribution cue or a docProps creator names appears in an input or the prompt.
  <sub>check_author_names_in_inputs · needs inputs, prompt, solution</sub>
- **G17** — A header line on the golden gives a named person only a title an input or the prompt states beside that name.
  <sub>check_header_titles_in_inputs · needs inputs, prompt, solution</sub>
- **G18** — A date on a golden row that names an owner stands in an input or the prompt.
  <sub>check_action_dates_anchored · needs inputs, prompt, solution</sub>
- **G19** — A prose claim that an item's quarter moves up or down agrees in sign with its correction column.
  <sub>check_direction_claims · needs solution</sub>
- **G20** — Every full date the golden states is carried by the prompt or an input, derived from one by a day count the record names, or set in a plan table's own date column.
  <sub>check_stated_dates_anchored · needs inputs, prompt, solution</sub>
- **G21** — Where one docx table column is a constant multiple of another, the printed figures multiply out at the displayed precision.
  <sub>check_docx_displayed_ratios · needs solution</sub>
- **G23** — A month-day date the golden spells out is carried verbatim by some input text or the prompt, never only by a workbook date cell.
  <sub>check_spelled_date_only_a_cell · needs inputs, prompt, solution</sub>
- **G24** — A typed numeric column in the golden that mirrors a per-key aggregate of an input CSV equals that aggregate on every key.
  <sub>check_typed_aggregate_drift · needs inputs, solution</sub>
- **G25** — An organisation or facility name a solution docx carries, in its body or in a header or footer part, is carried by some input text or the prompt.
  <sub>check_named_organisation_grounded · needs inputs, prompt, solution</sub>
- **G26** — A standard cited by number in the golden (NFPA 110, ISO 8528, UL 2200), and a class, type, level or tier stated in the same sentence as a standard (Class 72), is carried by the prompt or by some input, PDF inputs included.
  <sub>check_standard_references_grounded · needs inputs, prompt, solution</sub>
- **G28** — An organisation-style capitalised run in a solution docx that the prompt or an input carries only in lowercase, as a description, is written in that lowercase form; title-casing a description makes a name the grounding check cannot find.
  <sub>check_description_not_title_cased_into_name · needs inputs, prompt, solution</sub>
- **G29** — A banded lookup table in an input (rate, surcharge or tier rows written "a to b") whose adjacent rows share an endpoint states which row a value on the line takes, when a lookup value in the data lands exactly on a shared endpoint, because the golden's figure is otherwise not uniquely determined.
  <sub>check_band_boundary_convention · needs inputs</sub>
- **G30** — A golden docx table never sets a money column directly after a Yes/No column headed with a charge's name unless that money column is headed with the same name.
  <sub>check_yesno_charge_column_beside_money · needs solution</sub>
- **G31** — A golden sentence asserting an open status for every, all or each of the cited keys names the record it reads within six words of the status, whenever another input carries a closed status for one of those keys; the contradiction checker reads the claim without its sentence.
  <sub>check_universal_status_claim · needs inputs, solution</sub>
- **G33** — A golden's prose never states a per-group record count over a long CSV as a superlative claim ("the fewest visits at just 19"); the count lives in a ledgered table and the prose keeps the comparison.
  <sub>check_prose_superlative_count · needs inputs, solution</sub>
- **G34** — A prose cell that sends the reader to a named tab for an upper-case column label names a label that tab's header rows carry; a label that lives on another tab is a wrong direction a grader follows and reports.
  <sub>check_tab_header_citations · needs solution</sub>
- **G36** — A citation the golden writes as a document noun and a number ("policy 4.1", "ITB 5") appears in that exact form in some input; a citation form the inputs never use is coined terminology the attribution check fails.
  <sub>check_coined_citation_terms · needs inputs, solution</sub>
- **G37** — An identifier two inputs carry with different figures beside it (an alternator feature code reused across data sheets) is scoped, in every golden paragraph or table cell that cites it with one input's figure, by that input's file name or a token only that file's name carries.
  <sub>check_shared_identifier_scoped · needs inputs, prompt, solution</sub>
- **G38** — Every second location the golden records for an item appears as a from-location on the golden's move list.
  <sub>check_second_locations_on_move_list · needs solution</sub>
- **G39** — Two tables in a solution document that key their rows on the same first-column label never carry an identically named value column with different figures for the same row key; the numeric grounding audit reads the pair as one figure stated twice and reports an internal contradiction.
  <sub>check_shared_value_header_across_tables · needs solution</sub>
- **G40** — A read-through cell's row label never points the opposite way from the label on the row it references.
  <sub>check_readthrough_label_direction · needs solution</sub>
- **G41** — The golden delivers every artifact the prompt asks for inside the deliverable itself: no action row hands the making or form-filing of an asked-for artifact to later work, and where an input requires one form per record the golden carries one form block per record rather than a single schedule.
  <sub>check_deferred_deliverable · needs inputs, prompt, solution</sub>
- **G42** — No phrase recorded in the task's struck-phrases.md, as a literal or a pattern, appears anywhere in the prompt, the rubric, the Section 3 text, an input or a solution file, table cells and headers included.
  <sub>check_struck_phrases · needs folder</sub>
- **G43** — The task folder carries verify_golden.py, which re-derives the golden's figures from the inputs alone and exits 0 with every figure reproduced, and every standing a reasonable alternate reading flips names a convention phrase the golden itself states.
  <sub>check_golden_verification · needs folder, solution</sub>
- **R29** — A criterion stating how a figure is derived names a figure that sits in one golden cell only.
  <sub>check_derivation_anchors · needs rubric, solution</sub>
- **R71** — A universal deadline pin has no later-dated row in the golden's schedule.
  <sub>check_docx_deadline_pins · needs rubric, solution</sub>
- **R130** — A positive criterion that cites a numbered section ("Section 3.2") names a section the golden's own text cites somewhere; a section the golden never names fails the criterion on every oracle run, whatever the golden says in substance.
  <sub>check_cited_section_in_golden · needs rubric, solution</sub>
- **S1** — Every superlative in a solution text cell is re-verified against the table it summarises whenever the numbers move.
  <sub>check_superlatives · needs solution</sub>

### 2b llm_authorship_check (mean < 3.5 → NEEDS_REVISION)

#### LLM-PKG: Package metadata reads as something a person made in Excel and Word.

- **A1** — No shipped file uses a fill, shading or slide colour in the AI-blue family.
  <sub>check_blue_fills · needs inputs, solution</sub>
- **A2** — no datetime-formatted cells (a date is stored as plain text in the source format)
  <sub>check_datetime_and_walls · needs solution</sub>
- **A3** — creator is a sourced person or company (never empty, never a tool), a solution workbook has a title, created precedes modified, and no two files share a created stamp
  <sub>check_docprops · needs inputs, solution</sub>
- **A13** — A solution workbook with formulas ships xl/calcChain.xml.
  <sub>check_calcchain · needs solution</sub>
- **A14** — No shipped Office file names a python generator (Openpyxl, python-docx) in its docProps.
  <sub>check_generator_string · needs inputs, solution</sub>
- **A17** — Every formula cell carries exactly one cached <v>.
  <sub>check_caches · needs solution</sub>
- **A19** — creator and lastModifiedBy are never a placeholder such as 'generated' or 'unknown'
  <sub>check_docprops · needs inputs, solution</sub>
- **G2b** — Two or more input workbooks never share one zip write instant other than the 1980 epoch real Excel stamps.
  <sub>check_input_authorship · needs inputs, originality</sub>

#### LLM-SHAPE: Deliverable SHAPE, not word choice, decides this check.

- **A5** — no paste wall: a big tab that is near-letterless, repeats a sentence-length column, holds over 72% of the extracted text, or is a verbatim input extract over 30% of it
  <sub>check_datetime_and_walls · needs solution</sub>
- **A15** — No input CSV column repeats a strict short cycle or holds a near-uniform amount distribution.
  <sub>check_number_series · needs inputs</sub>

#### LLM-NUM: Numeric residue is human-scale.

- **A8** — No stored value carries more precision than its own number format shows.
  <sub>check_hidden_precision · needs solution</sub>
- **A9** — Every cached value is what the cell's own formula computes.
  <sub>check_stored_arithmetic · needs solution</sub>
- **A22** — A duration or time column in an input never holds only multiples of 2 or 5 across forty or more values; a computed time that is always clean reads as generated duration-first.
  <sub>check_clean_duration_column · needs inputs</sub>
- **A23** — A money column in an input never holds only exact multiples of 500 across twenty or more values; the authorship reader lists it as suspiciously clean and it co-occurs into a fail.
  <sub>check_round_money_column · needs inputs</sub>
- **A24** — A name column in an input never runs mostly on the nature-word plus landscape-feature plus trade-descriptor pattern (Ferncrest Mechanical, Brookhaven HVAC Parts); the authorship reader knows that lexicon.
  <sub>check_nature_name_column · needs inputs</sub>
- **A27** — A date column in an input never puts eight in ten of forty or more dates, over six or more months, on one day of the month that is neither the first nor the month's last; a fixed safe day is generator residue.
  <sub>check_fixed_day_dates · needs inputs</sub>
- **A28** — A product-name column in an input never carries the same line name under three or more competing brands for five or more lines; one scent or line vocabulary spread across every brand is the generator's, not the market's.
  <sub>check_cross_brand_line_names · needs inputs</sub>
- **A29** — A date column in an input never carries one date on nineteen in twenty of forty or more rows; an event column with no variation reads as a template stamp, not a record.
  <sub>check_single_valued_date_column · needs inputs</sub>
- **A30** — A quantity or stock column in a stock, count or master input never draws a hundred or more values from seven or fewer distinct figures; counted stock spreads, a generated set repeats, while an ordered quantity on a document line file (invoice, order, ticket) is a case-pack figure and stays out.
  <sub>check_discrete_quantity_set · needs inputs</sub>
- **A31** — A key column, the first in an input CSV, never runs forty or more ids on one constant step larger than one (PN-30100, PN-30107, PN-30114); a numbering series is issued one at a time or with gaps, never on a fixed stride.
  <sub>check_constant_step_ids · needs inputs</sub>
- **A55** — A money column in an input CSV whose rows carry cents never sums to a whole-dollar total for most of its keys (vendor, branch, account); ledger lines that land on round totals read as monthly figures engineered to hit predetermined targets.
  <sub>check_round_group_totals · needs inputs</sub>
- **F1** — No cached value in any task workbook is a float-repr artifact.
  <sub>check_floats · needs inputs, solution</sub>

### 2c agentic_rubric_quality_check (grounding, coverage, atomicity, objectivity, redundancy, unjustified rigidity)

#### RUBQ-ATOM: One measurement per criterion.

- **R27** — a positive asserts at most three separate figures, three only as steps of one computation
  <sub>check_atomicity · needs rubric</sub>
- **R49** — a positive names one item code, one account, and never a count beside its money figure
  <sub>check_atomicity · needs rubric</sub>
- **R54** — a positive carries no second scorable turn (a second sentence, a trailing so/including clause, a figured parenthetical, a three-parameter rule, a value beside its own derivation)
  <sub>check_atomicity · needs rubric</sub>
- **R55** — a criterion is one sentence of at most 45 words with no colon, semicolon or comma-and second predicate
  <sub>check_atomicity · needs rubric</sub>
- **R82** — A money figure in a criterion carries a currency mark and is written to the cent.
  <sub>check_currency_notation · needs rubric</sub>
- **R85** — a positive stacks no two comma-appended absolute clauses, and hangs no named example off a liveness clause
  <sub>check_atomicity · needs rubric</sub>
- **R105** — an ordering claim ('leads with', 'opens with') names one object, never two determiner-led objects
  <sub>check_atomicity · needs rubric</sub>

#### RUBQ-SCHEMA: The rubric grades the answer, not the golden's own layout.

- **R112** — A criterion names a golden worksheet only when the prompt or an input mandates that name; a bare golden-only tab name in prose is schema imposition the Rubric Quality Review fails as unjustified rigidity.
  <sub>check_golden_only_tab_name · needs inputs, prompt, rubric, solution</sub>
- **W12** — no golden Sheet!Cell reference or private column name the prompt never mandates
  <sub>check_rubric_lint · needs rubric</sub>

#### RUBQ-GROUND: Every id and figure a criterion pins is readable inside the review's input window.

- **R107** — a record id a positive pins sits only past row 28 of the input sheets and in no docx or the prompt
  <sub>check_ids_inside_review_window · needs inputs, rubric</sub>
- **R110** — a positive claims the deliverable cites records for a key (a SKU, an account) whose rows in some input sheet all sit past row 28
  <sub>check_ids_inside_review_window · needs inputs, rubric</sub>

#### RUBQ-WEIGHT: A core deliverable's cluster is weighted like a core deliverable, and no two strict rows score one total.

- **R114** — The strict liveness rows pin figures from different deliverables' outputs; two strict rows whose figures sit on one total row of one sheet score one requirement twice (redundant_or_double_counted to the Rubric Quality Review).
  <sub>check_strict_pair_one_total_row · needs rubric, solution</sub>

#### RUBQ-RIGID: Exact-number and exact-ID criteria stay under 80% of the rubric.

- **R81** — A positive action row never fixes the owner to a named person, and it scores that an owner is named and a date only when the prompt asks who and by when (R100 forbids both otherwise).
  <sub>check_named_owner · needs rubric</sub>
- **R100** — An action row scores an owner or a date only when the prompt asked who and by when.
  <sub>check_unmandated_owner_clause · needs prompt, rubric</sub>
- **R121** — A positive never presents a named answer as "one acceptable answer" or "for example"; the Rubric Quality Review reads the named answer as the row's key and every valid alternative as failing it.
  <sub>check_named_acceptable_answer · needs rubric</sub>
- **R122** — A positive never scores one business disposition against a named alternative; it scores that a disposition is stated and the finding it rests on, and leaves the choice to the solver.
  <sub>check_mandated_disposition · needs rubric</sub>
- **R128** — A positive names the thing counted or summed and its value, never one spreadsheet function as the required mechanism nor the exact words of its label, unless the same sentence admits any equivalent.
  <sub>check_function_or_label_lock · needs rubric</sub>
- **R129** — Formula and cell-reference criteria, strict and gated together, hold a quarter of positive weight or less when the prompt makes no live-formula demand; a larger share reads as grading implementation the prompt never asked for and failing a numerically correct static workbook.
  <sub>check_implementation_share · needs prompt, rubric, solution</sub>

### 2d dataset_quality_check / self_containment_check / golden_solution_leakage_check

#### DATA-SUFF: Every input file carries enough substance to stand on its own.

- **A11** — Every prose input document carries at least the word floor the platform's thinness axis applies.
  <sub>check_prose_source_floor · needs inputs</sub>
- **A12** — Each input spreadsheet or CSV holds at least 100 populated cells, and never fewer than 25.
  <sub>check_input_thinness · needs inputs, solution</sub>

#### DATA-OPEN: Every shipped package opens in the tooling a solver would reach for.

- **A16** — Every packaged Office file parses: each XML part is well-formed and openpyxl opens each workbook.
  <sub>check_package_parses · needs inputs, solution</sub>

#### DATA-BUDGET: The deliverable fits in the reviewing agent's context.

- **A7** — The deliverable's extracted text stays within what an agentic reviewer can hold in one read.
  <sub>check_reviewer_budget · needs solution</sub>

#### DATA-TIME: Dates are absolute and genuinely prospective.

- **A18** — no input records a completed event dated after the solution's own created stamp
  <sub>check_term_dates · needs inputs, solution</sub>
- **G22** — A snapshot input stamped with a date never references records another input dates after it, and a stated as-of balance equals the date-filtered register sum.
  <sub>check_cutoff_chronology · needs inputs</sub>
- **R23** — a firm-price or validity date (or relative window) stated in an input appears in a solution cell
  <sub>check_term_dates · needs inputs, solution</sub>
- **W2** — a relative time window carries an absolute date anchor
  <sub>check_rubric_lint · needs rubric</sub>

#### DATA-JOIN: Records the golden cites join across the inputs on the keys they carry.

- **G27** — A record the golden cites carries no related-record id of another input's key family that the input set does not hold.
  <sub>check_cited_record_joins · needs inputs, solution</sub>

#### DATA-LEAK: No input passage enumerates the golden's own answers.

- **L1** — No input paragraph enumerates manufacturers across the golden's answer set.
  <sub>check_leaked_enumeration · needs inputs, solution</sub>
- **L2** — No answer content hides where a visible-page read never shows it.
  <sub>check_hidden_content · needs inputs, solution</sub>
- **L3** — No input paragraph hands over an entity's stay or no-change verdict that a criterion scores.
  <sub>check_leaked_stay_verdict · needs inputs, rubric</sub>
- **L4** — No input paragraph names together every item a positive criterion scores as the listed or flagged set.
  <sub>check_leaked_listed_items · needs inputs, rubric</sub>
- **L5** — No input file is the deliverable or a copy of it: no input shares a golden file's basename, and no input workbook carries three or more of a golden workbook's sheet titles.
  <sub>check_golden_among_inputs · needs inputs, solution</sub>
- **L6** — A note or comment cell on an input row the golden cites carries no conclusion word (timing, duplicate, unexplained, cannot tie, no quantity change, hold until, should); the leakage check reads such a note as the answer handed over.
  <sub>check_conclusion_in_cited_note · needs inputs, solution</sub>
- **T1** — A tell log is a standalone file, never inside the shipped packages.
  <sub>check_tell_log · needs folder, inputs, solution</sub>

## Stage 3 · Human reviewer (max 5 total reviews, then accept/reject only)

*after the evals clear; costs a review round and reviewer trust.*

### 3 Human reviewer findings

#### REV-PROSE: No LLM-style prose: invented idiom, slogans, or the three-beat sentence rhythm.

- **A6** — No input or solution .docx carries em dashes.
  <sub>check_emdash_docx · needs inputs, solution</sub>
- **A10** — The prompt, inputs and golden carry none of the reviewer-flagged LLM prose shapes.
  <sub>check_llm_prose · needs inputs, prompt, solution</sub>
- **A21** — A document never carries the status-report sentence skeleton (remained / continue / completed according to procedure / no X was reported) on half or more of its sentences.
  <sub>check_status_template_register · needs inputs, solution</sub>
- **A25** — A note or comment column in a tabular input, filled on nine rows in ten, never draws on twelve or fewer distinct phrases across a hundred rows or more, nor on four or fewer across forty or more; the authorship reader names that a uniform log-entry template.
  <sub>check_uniform_note_column · needs inputs</sub>
- **A26** — A free-text column on a solution worksheet, populated on three rows or more, never carries one identical multi-word phrase on every populated row; the authorship reader names that a uniform log template whatever the row count.
  <sub>check_uniform_solution_text_column · needs inputs, solution</sub>
- **A32** — A solution docx never carries two or more headings from the generic report outline (Executive Summary, Recommendations, Introduction, Overview, Background, Conclusion, Next Steps, Key Findings, Methodology); the authorship reader reads the pair as a model's outline and fails the golden on it.
  <sub>check_generic_heading_pair · needs solution</sub>

#### REV-REGISTER: The prose is desk register throughout — the prompt, the inputs AND the golden.

- **A20** — A sentence puts a comma before a clause-joining conjunction and joins no more than two clauses.
  <sub>check_run_on_sentences · needs prompt</sub>
- **R84** — A negative states its prohibited act once with the frame inside the sentence, never ending on a trailing rule-breach clause such as 'in violation of' or 'contrary to'.
  <sub>check_negative_trailing_clause · needs rubric</sub>

#### REV-CREDIBLE: Names, dates and document metadata survive being checked.

- **H2** — no 555-prefix phone number
  <sub>check_hygiene · needs inputs, solution</sub>
- **H3** — no weekday/date pair that is calendar-false
  <sub>check_hygiene · needs inputs, solution</sub>
- **P8** — A due date the prompt gives for the deliverable, and a Date line on the golden document, fall at least 21 days after the task was built, so the in-world deadline has not already passed when the task is read.
  <sub>check_due_date_ahead_of_review · needs folder, prompt</sub>

#### REV-TRACE: Every basis the golden states is the input's own basis, and every numbered rule in an input memo is graded.

- *no detector; a human check* (see gate_families.NO_DETECTOR)

## Procedural rules (not mechanically checked)

House rules with no detector, registered in tools/gcheck/procedural.py so they carry an id.

- **PR1** — Keep every worked example inside the first 25 data rows of an input and never pin a count a partial read cannot reproduce: the Rubric Quality Review reads inputs through a 28-row window (28 observed, 25 prescribed, a cushion of 3); the record-id arm is coded as R107, counts and quantities stay by hand.
  <sub>memory/rubric-coverage-and-completeness.md (2026-09-01; 2026-09-03)</sub>
- **PR2** — Run no stb command in a revision; accept, revise or skip is never issued from this desk.
  <sub>memory/revision-workflow.md</sub>
- **PR3** — Package in this order: tools/office_resave.py --force, then fix_floats, then fix_metadata, then build the zips, then run the gate.
  <sub>memory/package-hygiene-pipeline.md (2026-09-02)</sub>
- **PR4** — A revision runs under the model recorded in the task's metadata.json built_with.
  <sub>memory/model-routing.md</sub>
- **PR6** — When a golden's figures are totals over an input longer than the review window, the deliverable carries a per-key ledger (account, route) whose rows sum to the stated totals and names the record ids behind any single-account figure, so a partial read can still trace them (2026-09-11: a grader read about forty of 117 log rows and called every route total fabricated).
  <sub>memory/golden-fidelity.md (2026-09-11)</sub>
- **PR7** — The prompt's experience sentence (P4c) is written fresh for the task: the portfolio's stock phrasings ('a few years of X behind you/them', 'this is coming to you because') sit in sixteen prompts, and a re-entered prompt carrying them failed the platform's in-app prompt uniqueness check where the task's earlier wording had passed (2026-09-11).
  <sub>house rule since 2026-09-11. Uniqueness is checked by hand against this desk's own drafts, submissions and accepted work, with the occupation counting as part of a task's identity</sub>
- **PR9** — A criterion grading a computation an input clause defines names every factor the clause states (rate, base and period alike): a row naming fewer factors credits a formula the clause forbids, and adjudication reads the row against the clause (2026-09-12: 'the order total at the policy carrying rate' let a flat 18% formula earn the point where policy 7.2 halves the coverage period).
  <sub>memory/rubric-liveness-criteria.md (2026-09-12)</sub>
- **PR10** — Every person the prompt names carries their role beside their name in an input (a signature line, an addressee line, 'Gail, my purchasing manager'); a role the reader joins across two sentences or two documents is inferred, and adjudication traces it as unsourced whether or not the golden states the title (2026-09-11 to 09-14: on one task the title was removed in an early round and the trace returned twice more until the email stated the role beside the name). G17 catches the title on a header line; the untitled inference is read by hand.
  <sub>memory/golden-fidelity.md (2026-09-14)</sub>
- **PR8** — A deliverable byline names only the recipient an input establishes and the plan date; it carries no preparer clause, by person or by department, unless an input assigns that role, because adjudication traces the clause to the one person the inputs place in that department (2026-09-12: 'Prepared for purchasing' was read back to a person the terms sheet names only as its checker).
  <sub>memory/golden-fidelity.md (2026-09-12)</sub>
- **PR11** — The Section 3 paragraph (review-comment.md) never introduces a person the deliverable does not name: the adjudicator's trace reads it as a claim source and quoted the submitter's own sentence about the terms sheet's checker back as a finding after the workbook itself was clean (2026-09-14); describe such a person by role.
  <sub>memory/revision-workflow.md (2026-09-14)</sub>
- **PR12** — A single-key figure the golden builds from a few rows of a long CSV (an account's own drive and selling minutes over eight visits) has those rows inside adjudication's preview window, the first 25 and last 15 lines, by relaying the file into side-by-side panels or one row per key, never by moving the key's rows to the top: a lead block hands the solver the scope decision by file order (2026-09-15), and cited ids alone are called untraceable when the rows are hidden (2026-09-14). H8 codes the cited-row arm; a broad rule over every register id lit up seven goldens that cite ids legitimately deep in their CSVs.
  <sub>memory/golden-fidelity.md (2026-09-14, 2026-09-15)</sub>
- **PR13** — A rubric row grades only an action inside the scope the prompt sets for actions ("what we change on our own dock"), never a planning choice the golden adds on its own (a filing date ahead of a meeting, a written confirmation asked of the carrier): adjudication reads those rows as grading operational choices the prompt never requests (2026-09-14). The golden may still carry the choice as content; the rubric may not score it. Fuzzy to code: the scope phrase differs per prompt.
  <sub>memory/rubric-coverage-and-completeness.md (2026-09-14)</sub>
- **PR14** — Before every resubmission the platform's input file list holds exactly the i- zip's members and never the deliverable: the input widget keeps every upload it was ever given, and an s- zip dropped there ships the golden as an input; the fetch-task JSON's input_files list is the read-back (an adjudication, 2026-09-14; L5 catches a staged copy, nothing in the repo can see the widget).
  <sub>memory/revision-workflow.md (2026-09-14)</sub>
- **PR15** — A hedged per-item finding in the golden ('if one request is a duplicate, 110 units stay unexplained') is restated elsewhere only in the same hedged form: no later sentence turns it into a definite position or direction (a 'true position', 'would overstate the need'); the internal-contradiction check reads every summary-level claim against the detail and lists the pair.
  <sub>a late revision round's feedback log (2026-09-14)</sub>
- **PR16** — A rubric row that asks which items of a schedule fall in a class ('which of the corrective actions are changes to a procedure rather than re-approvals') is met in the golden by a per-row marker column beside the items (a Yes/No 'Procedure change' column), never by a prose sentence listing the members above the table: the oracle judge reads the table for the per-row attribute and failed the prose form 1 of 3 runs (2026-09-14). The rubric row is worded per item ('for each item, whether it is ...'). One hit portfolio-wide, so it stays procedural.
  <sub>memory/rubric-anchoring-and-landing.md (2026-09-14)</sub>
- **PR17** — An adjudication or review note can be written against a version the platform no longer holds: before changing anything, read the note's cited content (population figures, names, counts, criteria it says are missing) against the live copy (a pasted fetch-task JSON). An item the live copy already answers is a trace-lag item: change nothing for it, confirm on the form that the rows and files are there, and say so in the feedback log. (An adjudication, 2026-09-14: the note asked for three rubric rows and named an author that the platform's live copy already carried and had already dropped.)
  <sub>memory/revision-workflow.md (2026-09-14)</sub>
- **PR18** — Every zip-upload form action tells the operator to confirm the upload's uploadedAt moved, and a round closes only after a re-fetch shows both zip stamps on the round's day: one revision round (2026-09-14) had its criteria re-entered while the zips never went up, and the next adjudication note was written against an earlier round's golden and the first round's inputs.
  <sub>memory/revision-workflow.md (2026-09-14)</sub>
- **PR19** — From a task's third return, the revision rebuilds the package from clause-map.md: every prompt ask is re-read against the golden and the rubric, every finding from earlier rounds is re-checked in every file, and only then are the items the new note names fixed; patching the named items alone let one task carry an unmet ask through eleven rounds to rejection (2026-09-15). R134 checks the map's Rebuilt date.
  <sub>memory/revision-workflow.md (2026-09-15)</sub>
- **PR20** — Every phrase a reviewer or check strikes, and every claim a fix removes, is recorded the same day as a bullet in the task's struck-phrases.md, as a literal in double quotes or a /pattern/ that also catches the claim reworded, followed by its source and date; G42 then searches the whole package for it (2026-09-15: a struck Appendix B claim survived in a table cell).
  <sub>memory/revision-workflow.md (2026-09-15)</sub>
